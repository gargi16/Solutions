#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int a[n + 1] ={0};
    int p[n + 1];
    vector<int> v;
    int root;
    bool visited[n + 1] = {false};
    vector<int> :: iterator it ;

    for (int i = 1; i < n; i++) {
        int y, z;
        cin >> y >> z;

    }

    for (int i = 1; i <= m; i++) {
        int y, z;
        cin >> y >> z;

    }
    return 0;
}